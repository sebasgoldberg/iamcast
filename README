INSTALL
-------

1) Install the package ciudades.
2) cp alternativa/ambiente.default.py to ambiente.py and apply your configuration.
2) Run sudo ./install.sh (for iamcast).
3) Verify that ports for apache2 are configured correctly.
